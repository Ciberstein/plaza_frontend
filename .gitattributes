# Normalise line endings in the repository. Without this every file committed
# from Windows lands with CRLF and a checkout elsewhere shows the whole file as
# changed, which buries real diffs.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.svg text eol=lf
*.woff binary
*.woff2 binary
