export const APP_NAME = 'Plaza'

// Categories, cities and delivery options are not listed here on purpose: the
// API serves them from /public/meta, which is also what it validates against.
// A second copy in the frontend is a copy that drifts.
